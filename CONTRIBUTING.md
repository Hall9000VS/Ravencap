# Contributing

## Workflow

1. Create a feature branch from `main`.
2. Keep the workspace building with `cargo fmt`, `cargo clippy`, and `cargo test`.
3. Open a pull request with scope, risk, and validation notes.

## Current Focus

The repository is in scaffolding mode. Prefer small, isolated changes that advance the Phase 0.5 compatibility gate or the Phase 1 raw stream implementation.
